inside the source folder there are WEBAPI is a .net project as a backend , Frontend is angular project.
first go to the webconfig file and change the path name for the csv files actual location. 
and run the project copy the url and paste it inside the angular project folder src>sharde>user.service.ts readonly type variable replace the url and run it.